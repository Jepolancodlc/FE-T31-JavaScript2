
function mensaje(tipo) {
    switch (tipo) {
        case (1): alert("Se presionó un párrafo del documento."); break;
        case (2): alert("Se presionó un párrafo contenido en la segunda tabla."); break;

    }
}
